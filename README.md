# UrbanStyle - Интернет-магазин одежды

Flutter приложение интернет-магазина с авторизацией, каталогом товаров, корзиной и избранным.

## 🎨 Особенности

- **Темный минималистичный дизайн** (#121212 фон, #1E1E1E карточки)
- **Авторизация через Supabase** (регистрация, вход, выход)
- **Профиль пользователя** с отображением имени и инициалов
- **Каталог товаров** - 8 предметов одежды
- **Корзина** с управлением количеством и персональным хранением
- **Избранное** с сохранением для каждого пользователя
- **Навигация** через бургер-меню по 8 страницам

## 📁 Структура проекта

```
lib/
  ├── main.dart           # Главный файл приложения, все UI компоненты
  ├── models.dart         # Модели данных (Product, CartItem)
  ├── services.dart       # Сервисы (CartService, FavoritesService)
  └── supabase_config.dart # Конфигурация Supabase и AuthService
```

## 🚀 Запуск проекта

1. **Установите зависимости:**
   ```bash
   flutter pub get
   ```

2. **Настройте Supabase:**
   - Откройте `lib/supabase_config.dart`
   - Добавьте свои `SUPABASE_URL` и `SUPABASE_ANON_KEY`
   - Создайте таблицы в Supabase (см. комментарии в файле)

3. **Запустите приложение:**
   ```bash
   flutter run -d chrome    # Для веб-версии
   flutter run              # Для других платформ
   ```

## 📦 Зависимости

- `flutter` - Flutter SDK
- `supabase_flutter: ^2.0.0` - Авторизация и база данных
- `provider: ^6.0.0` - Управление состоянием
- `flutter_lints: ^2.0.0` - Линтинг кода

## 🗄️ Supabase таблицы

### user_carts
- `user_id` (uuid, FK к auth.users)
- `cart_data` (jsonb)
- `updated_at` (timestamp)

### user_favorites
- `user_id` (uuid, FK к auth.users)
- `product_ids` (text[])
- `updated_at` (timestamp)

## 🎯 Страницы

1. **Главная** - Баннер, галерея товаров, информация о бренде
2. **Каталог** - Сетка товаров с добавлением в корзину/избранное
3. **Акции** - Специальные предложения
4. **Избранное** - Сохраненные товары пользователя
5. **Корзина** - Товары с управлением количеством
6. **О нас** - Информация о магазине
7. **Контакты** - Контактная информация
8. **Настройки** - Настройки приложения

## 👨‍💻 Автор

GitHub: [@dssmdaria-cpu](https://github.com/dssmdaria-cpu)

## 📄 Лицензия

Учебный проект, 4 курс
